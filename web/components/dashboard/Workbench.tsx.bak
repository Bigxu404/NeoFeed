'use client';

export default function Workbench() {
  return (
    <div className="min-h-screen bg-[#050505] text-white flex flex-col items-center justify-center">
      <div className="p-8 border border-white/10 rounded-3xl bg-neutral-900/50 backdrop-blur-xl text-center">
        <h1 className="text-4xl font-serif italic mb-4 text-white">Workbench Debug</h1>
        <p className="text-white/40 mb-8">If you can see this, the routing and basic rendering are working.</p>
        <button 
          onClick={() => window.location.reload()}
          className="px-6 py-2 bg-white text-black rounded-lg text-sm font-bold"
        >
          Refresh
        </button>
      </div>
    </div>
  );
}

